# QuickWrite 第一期需求文档

## 当前进度概览

**完成度**: 90% (不含测试) | **状态**: 第一期主体开发完成 | **更新日期**: 2026-04-24

### 已完成功能 ✅
- [x] 项目基础架构搭建
- [x] MVVM架构实现（手动依赖注入）
- [x] Room数据库和表结构设计（3个实体、3个DAO）
- [x] 项目列表页面UI和功能（含空状态、项目卡片、排序对话框）
- [x] 项目创建页面UI和功能（含表单验证、封面图片选择）
- [x] 项目编辑页面UI和功能（加载已有项目数据、修改保存）
- [x] 项目详情查看页面（含封面管理、删除/编辑按钮、导出占位菜单）
- [x] 项目删除功能（长按触发 + 二次确认对话框 + 数据库和文件系统清理）
- [x] 项目排序功能（按修改时间、创建时间、名称排序）
- [x] 启动界面和闪屏（SplashScreen + QuickWriterApp 入口切换）
- [x] 底部导航和页面路由（10个路由页面 + NavHost导航）
- [x] 深蓝色主题系统配置（Material Design 3完整配色方案）
- [x] 文件系统管理工具（目录结构创建、文件读写、安全路径验证）
- [x] 封面图片处理工具（缩放裁剪、JPEG编码、统一存储管理）
- [x] 数据验证和错误处理框架（表单验证、Result返回类型、UI状态处理）
- [x] 系统设置界面（SettingsScreen 内部导航 + 多子页面）
- [x] AI模型配置管理系统（AiConfigScreen 列表/编辑/删除/设为默认）
- [x] 写作参数设置（温度、最大Token数、Top P参数配置）
- [x] 应用通用设置（主题模式/深色模式、字体大小、自动保存间隔）
- [x] 关于页面（版本号、构建信息、开发者信息、版权声明）
- [x] 全部5个ViewModel实现（含工厂类）
- [x] Room TypeConverters（List\<String\> 逗号分隔转换）
- [x] 用户设置数据管理（DataStore风格Key-Value存储）
- [x] markor-editor 模块导入（基于Markor HighlightingEditor移植）

### 进行中功能 🚧
- [ ] 网络请求层实现（Retrofit + OkHttp 服务搭建）

### 待开发功能 ⏳
- [ ] 编辑器字体类型选择
- [ ] 单元测试和集成测试

---

## 1. 项目概述

### 1.1 项目背景
QuickWrite是一款专为长篇小说创作者设计的Android原生应用程序，通过AI助理功能帮助作家系统化地管理复杂的小说创作过程。第一期聚焦于构建应用的基本框架和核心功能。

### 1.2 开发周期
**第一期开发周期**：6-8周
**目标**：构建稳定的项目管理系统和基础AI集成框架

### 1.3 技术栈
- **平台**：Android (API 24+)
- **开发语言**：Kotlin
- **架构**：MVVM (Model-View-ViewModel)
- **数据存储**：Room数据库 + 文件系统
- **网络**：Retrofit + OkHttp
- **UI框架**：Jetpack Compose
- **其他**：Kotlin协程、Flow

## 2. 第一期功能需求

### 2.1 应用入口界面 [进度: 100%]

#### 2.1.1 启动界面 [✅ 已完成]
- **闪屏页面**：应用logo和名称展示，含loading状态文字 ✅
- **初始化检查**：检查必要的文件目录和数据库 ✅
- **权限请求**：通过 Android 权限系统处理 ✅

#### 2.1.2 主界面布局 [✅ 已完成]
- **底部导航栏**：包含"项目"、"写作"、"设置"三个主要标签 ✅
- **顶部工具栏**：项目列表标题 + 排序按钮；FAB浮动按钮提供新建入口 ✅
- **主要内容区**：根据当前标签显示相应内容 ✅
- **写作页面**：当前为占位符页面 [⏳ 待实现]

### 2.2 项目管理系统 [进度: 95%]

#### 2.2.1 项目列表页面 [✅ 已完成]
- **空状态**：无项目时的引导界面 ✅
- **项目卡片**：每个项目以卡片形式展示，包含：
  - 项目封面（支持自定义图片）✅
  - 项目标题 ✅
  - 作者名称 ✅
  - 小说类型标签 ✅
  - 创建时间 ✅
  - 字数统计 ✅
  - 最近修改时间 ✅
- **排序功能**：按创建时间、修改时间、名称排序 ✅

#### 2.2.2 项目创建功能 [✅ 已完成]
- **创建入口**：主界面浮动按钮 ✅
- **表单字段**：
  - 项目标题（必填）✅
  - 作者名称（必填）✅
  - 小说类型选择（历史、奇幻、科幻、都市、玄幻等15种）✅
  - 项目描述（可选）✅
  - 封面图片（可选）✅
- **验证规则**：
  - 标题不能为空 ✅
  - 项目标题唯一性校验 ✅
  - 作者名称不能为空 ✅

#### 2.2.3 项目编辑功能 [✅ 已完成]
- **进入方式**：长按项目卡片进入编辑模式或点击项目详情中的编辑按钮 ✅
- **可编辑字段**：
  - 项目标题 ✅
  - 作者名称 ✅
  - 小说类型 ✅
  - 项目描述 ✅
  - 封面图片 ✅
- **更新机制**：修改后自动保存，同步更新项目目录结构 ✅

#### 2.2.4 项目删除功能 [✅ 已完成]
- **删除方式**：长按项目卡片显示删除选项 ✅
- **确认机制**：二次确认对话框 ✅
- **删除内容**：
  - 删除数据库记录 ✅
  - 删除对应的项目目录和所有文件 ✅
  - 清理相关的AI对话记录 [⏳ 待实现 - AI对话表尚未建立]
- **回收站**：可考虑后续添加回收站功能（第一期不包含）

#### 2.2.5 项目详情查看 [✅ 已完成]
- **进入方式**：点击项目卡片 ✅
- **显示内容**：
  - 项目基本信息（标题、作者、类型、描述、状态）✅
  - 封面图片管理（添加/替换/删除）✅
  - 创作统计（字数、章节数、创建/修改时间）✅
  - 快速操作按钮（编辑、删除、设为当前项目、导出菜单占位）✅
  - 文件结构预览 [⏳ 待实现]
  - 最近活动记录 [⏳ 待实现]

### 2.3 系统设置界面 [进度: 90%]

#### 2.3.1 AI模型配置 [✅ 已完成]
- **模型选择**：支持多AI服务商配置
  - OpenAI (GPT-3.5-turbo, GPT-4) ✅
  - Anthropic Claude ✅
  - 自定义API端点 ✅
- **配置项**：
  - API密钥设置 ✅
  - 基础URL（OpenAI兼容API）✅
  - 模型名称 ✅
  - 默认温度参数（0.1-2.0）✅
  - 最大token数（100-8000）✅
  - Top P参数（0-1）✅
  - Top K参数（1-100）✅
  - Frequency Penalty（0-2）✅
  - Presence Penalty（0-2）✅
- **配置管理**：新增/编辑/删除/设为默认 ✅

#### 2.3.2 创作参数设置 [✅ 已完成基本功能]
- **默认参数**：
  - 温度（控制创意程度）：默认0.8 ✅
  - 最大生成长度：默认2000字符 ✅
  - Top P参数 ✅
  - 是否使用模型预设配置 ✅
- **待完善项**：
  - 重复惩罚参数 [⏳ 待实现]
  - 频率惩罚参数 [⏳ 待实现]
- **项目级覆盖**：[⏳ 当前为全局设置]

#### 2.3.3 应用通用设置 [✅ 已完成基本功能]
- **主题设置**：
  - 浅色/深色模式 ✅
  - 跟随系统 ✅
- **字体设置**：
  - 编辑器字体大小（10-24sp滑块调节）✅
  - 编辑器字体类型 [⏳ 待实现]
- **自动保存**：
  - 自动保存间隔（1分钟、5分钟、10分钟）✅
  - 立即自动保存开关 ✅
  - 草稿保留时间 [⏳ 待实现]
- **存储管理**：[⏳ 第二期实现]
  - 显示存储空间使用情况
  - 清理缓存文件
  - 导出项目备份

#### 2.3.4 关于页面 [✅ 已完成]
- **应用信息**：
  - 版本号（1.0.0-alpha）✅
  - 构建日期（2026-04）✅
  - 构建号（100）✅
  - 开发者信息（Universe Software）✅
  - 最低API版本（24）和目标API（34）✅
- **帮助与支持**：[⏳ 第二期实现]

### 2.4 文件系统管理 [进度: 100%]

#### 2.4.1 项目目录结构创建 [✅ 已完成]
- **初始目录结构**：
  ```
  /{项目ID}/
  ├── 简介.md                # 项目基本信息文件
  ├── 正文/
  ├── 设定/
  │   ├── 人物/
  │   ├── 地点/
  │   ├── 组织/
  │   └── 物品/
  ├── 时间线/
  ├── 记录/
  └── 配置/
      ├── AI指令.md
      └── 写作规范.md
  ```
- **模板文件生成**：自动创建必要的模板文件（简介.md、AI指令.md、写作规范.md）✅

#### 2.4.2 文件读写系统 [✅ 已完成]
- **文件操作API**：
  - 创建文件/目录 ✅
  - 读取文件内容 ✅
  - 写入/更新文件内容 ✅
  - 删除文件/目录 ✅
  - 文件重命名 ✅
- **编码处理**：统一使用UTF-8编码 ✅
- **路径管理**：安全路径处理，防止目录遍历攻击 ✅
- **封面图片处理**：独立 CoverImageProcessor，600x800 缩放裁剪，JPEG编码 ✅
- **文件操作工具类**：AppUtils 提供文件名清理、格式转换等辅助功能 ✅

## 3. 界面设计规范 [进度: 100%]

### 3.1 颜色方案 [✅ 已完成]
- **主色调**：深蓝色 (#1a237e)
- **辅助色**：浅蓝色 (#2196f3)
- **强调色**：橙色 (#ff9800)
- **文本色**：
  - 主要文字：#212121
  - 次要文字：#757575
  - 禁用文字：#9e9e9e
- **背景色**：
  - 浅色背景：#ffffff
  - 深色背景：#121212

### 3.2 字体规范 [✅ 已完成]
- **英文字体**：Roboto
- **中文字体**：思源黑体 / Noto Sans SC
- **字号规范**：
  - 标题：20sp
  - 副标题：16sp
  - 正文：14sp
  - 辅助文本：12sp

### 3.3 组件规范 [✅ 已实现]
- **按钮**：
  - 主要按钮：填充按钮，圆角8dp ✅
  - 次要按钮：边框按钮，圆角8dp ✅
  - 文本按钮：纯文本，无边框 ✅
- **卡片**：
  - 圆角：12dp ✅
  - 阴影：2dp elevation ✅
  - 内外边距：16dp ✅
- **输入框**：
  - 下划线风格 ✅
  - 错误状态提示 ✅
  - 悬浮标签 ✅

## 4. 数据库设计 [进度: 90%]

### 4.1 核心表结构（Room数据库方案）[✅ 已完成]

```kotlin
// 项目表实体类
@Entity(tableName = "projects")
data class ProjectEntity(
    @PrimaryKey
    val id: String,
    @ColumnInfo(name = "title")
    val title: String,
    @ColumnInfo(name = "author")
    val author: String,
    @ColumnInfo(name = "genre")
    val genre: String,
    @ColumnInfo(name = "description")
    val description: String?,
    @ColumnInfo(name = "created_time")
    val createdTime: Long,
    @ColumnInfo(name = "modified_time")
    val modifiedTime: Long,
    @ColumnInfo(name = "status")
    val status: String,
    @ColumnInfo(name = "cover_image_path")
    val coverImagePath: String?,
    @ColumnInfo(name = "word_count")
    val wordCount: Int = 0,
    @ColumnInfo(name = "chapter_count")
    val chapterCount: Int = 0,
    @ColumnInfo(name = "storage_path")
    val storagePath: String
)

// AI模型配置表实体类
@Entity(tableName = "ai_model_configs")
data class AiModelConfigEntity(
    @PrimaryKey(autoGenerate = true)
    val id: Int = 0,
    @ColumnInfo(name = "config_name")
    val configName: String,
    @ColumnInfo(name = "provider")
    val provider: String,
    @ColumnInfo(name = "api_key")
    val apiKey: String,
    @ColumnInfo(name = "base_url")
    val baseUrl: String?,
    @ColumnInfo(name = "model_name")
    val modelName: String,
    @ColumnInfo(name = "temperature")
    val temperature: Float = 0.7f,
    @ColumnInfo(name = "max_tokens")
    val maxTokens: Int = 2000,
    @ColumnInfo(name = "top_p")
    val topP: Float = 1.0f,
    @ColumnInfo(name = "top_k")
    val topK: Int = 50,
    @ColumnInfo(name = "frequency_penalty")
    val frequencyPenalty: Float = 0.0f,
    @ColumnInfo(name = "presence_penalty")
    val presencePenalty: Float = 0.0f,
    @ColumnInfo(name = "is_default")
    val isDefault: Boolean = false
)

// 用户设置表实体类
@Entity(tableName = "user_settings")
data class UserSettingEntity(
    @PrimaryKey
    @ColumnInfo(name = "key")
    val key: String,
    @ColumnInfo(name = "value")
    val value: String,
    @ColumnInfo(name = "category")
    val category: String
)

// 数据访问对象（DAO）接口示例
@Dao
interface ProjectDao {
    @Query("SELECT * FROM projects ORDER BY modified_time DESC")
    fun getAllProjects(): Flow<List<ProjectEntity>>
    
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertProject(project: ProjectEntity)
    
    @Update
    suspend fun updateProject(project: ProjectEntity)
    
    @Delete
    suspend fun deleteProject(project: ProjectEntity)
    
    @Query("SELECT * FROM projects WHERE id = :id")
    suspend fun getProjectById(id: String): ProjectEntity?
}

// Room数据库类
@Database(
    entities = [
        ProjectEntity::class,
        AiModelConfigEntity::class,
        UserSettingEntity::class
    ],
    version = 1,
    exportSchema = false
)
abstract class AppDatabase : RoomDatabase() {
    abstract fun projectDao(): ProjectDao
    abstract fun aiModelConfigDao(): AiModelConfigDao
    abstract fun userSettingDao(): UserSettingDao
}
```

### 4.2 数据初始化
- **首次启动**：创建数据库和必要的表 ✅
- **默认配置**：插入预设的AI模型配置 [⏳ 待实现 - 当前无数据库Callback]
- **必要设置**：初始化默认用户设置 [⏳ 待实现 - 访问时自动创建默认值]

## 5. 技术实现要点 [进度: 90%]

### 5.1 架构设计 [✅ 已完成]
```
┌─────────────────────────────────────────────┐
│           Presentation Layer                │
│   Composable Screens / ViewModels           │
│   LiveData / State Flow / MutableState      │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│            Domain Layer                     │
│      UseCases / Repositories / Models       │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│             Data Layer                      │
│   Repositories / Room Database / DataSources│
└─────────────────────────────────────────────┘
```

### 5.2 核心UseCase定义 [✅ 已完成]
1. **ProjectManagementUseCase**：项目增删改查、排序、统计数据、封面管理 ✅
2. **FileSystemUseCase**：文件操作管理（内置于ProjectManagementUseCase）✅
3. **AIConfigUseCase**：AI配置管理（内置于SettingsUseCase）✅
4. **SettingsUseCase**：应用设置管理 + AI配置CRUD + 当前项目管理 ✅

### 5.3 关键实现细节
1. **文件系统安全性** [✅ 已完成]：
    - 文件路径验证 ✅
    - 权限检查 ✅
    - 异常处理 ✅

2. **异步处理** [✅ 已完成]：
    - 使用协程处理IO操作 ✅
    - 避免主线程阻塞 ✅

## 6. 测试要求 [进度: 0%]

### 6.1 单元测试 [⏳ 未开始]
- **ViewModel测试**：项目列表、创建、编辑逻辑
- **UseCase测试**：核心业务逻辑
- **Repository测试**：数据访问层

### 6.2 集成测试 [⏳ 未开始]
- **数据库操作**：增删改查功能
- **文件系统操作**：文件读写、目录创建
- **设置管理**：配置保存和读取

### 6.3 UI测试 [⏳ 未开始]
- **项目列表**：滚动、点击、长按操作
- **项目创建**：表单填写、提交验证
- **系统设置**：配置项修改和保存

### 6.1 单元测试
- **ViewModel测试**：项目列表、创建、编辑逻辑
- **UseCase测试**：核心业务逻辑
- **Repository测试**：数据访问层

### 6.2 集成测试
- **数据库操作**：增删改查功能
- **文件系统操作**：文件读写、目录创建
- **设置管理**：配置保存和读取

### 6.3 UI测试
- **项目列表**：滚动、点击、长按操作
- **项目创建**：表单填写、提交验证
- **系统设置**：配置项修改和保存

## 7. 部署和发布

### 7.1 开发阶段
- **Alpha版本**：内部测试和功能验证
- **Beta版本**：小范围用户测试
- **Release版本**：正式发布

### 7.2 发布准备
- **应用图标**：适配各分辨率的图标
- **应用截图**：准备应用商店截图
- **应用描述**：编写详细的应用描述
- **隐私政策**：准备隐私政策文档

## 8. 后续计划衔接

### 8.1 第二期开发准备
- 第一期代码结构和架构为后续开发打好基础
- 文件系统管理模块可以直接用于后续的文档编辑功能
- AI配置管理模块为AI写作功能提供支持

### 8.2 扩展点
- 项目导入导出功能
- 云同步备份功能
- 多人协作功能（远期规划）

---

**文档版本**：2.0 | **最后更新**：2026-04-24

## 开发进度记录

### 2026-04-24 开发更新 - 第一期主体功能开发完成
- ✅ 完成全部10个UI页面（Splash + 5个项目管理 + 设置集 + 关于）
- ✅ 实现5个ViewModel的完整业务逻辑和状态管理
- ✅ 实现项目排序功能（修改时间/创建时间/名称）
- ✅ 实现项目编辑功能（完整CRUD）
- ✅ 实现项目详情查看页面（含封面管理、统计数据）
- ✅ 实现AI模型配置管理系统（多服务商、完整参数配置）
- ✅ 实现系统设置集（主题/字体/自动保存/写作参数）
- ✅ 实现启动界面和闪屏
- ✅ 实现关于页面
- ✅ 导入markor-editor编辑器模块
- ✅ 完善数据验证和错误处理框架
- 🚧 构建状态：BUILD SUCCESSFUL

### 2026-04-21 开发更新
- ✅ 完成第一季度核心架构搭建
- ✅ 实现项目管理系统基础功能（列表、创建、删除）
- ✅ 配置深蓝色主题和Material Design 3规范
- ✅ 建立完整的MVVM架构和依赖注入
- ✅ 完成Room数据库和文件系统管理

### 技术实现说明
- **依赖注入**：采用手动依赖注入（AppContainer）替代Hilt
- **API存储**：API密钥使用DataStore直接存储，不进行加密（已知技术债务）
- **架构模式**：严格的MVVM架构，分为表现层、领域层、数据层
- **状态管理**：使用StateFlow和MutableState管理UI状态
- **导航方案**：Jetpack Navigation Compose + SettingsScreen内部状态路由
- **排序实现**：ProjectDao提供3种不同排序查询 + ViewModel切换

### 下一步计划（第二期）
1. 实现网络请求层（Retrofit + OkHttp AI服务集成）
2. 开发正文章节编辑器和Markdown编辑器
3. 搭建小说设定管理界面（人物/地点/组织/物品）
4. 开发时间线管理功能
5. 实现导出和分享功能
6. 编写单元测试和集成测试
7. 清理技术债务（数据库迁移策略、API密钥加密）

### 已知问题和技术债务
- Room数据库构建器使用了已弃用的`fallbackToDestructiveMigration()`
- API密钥使用DataStore直接存储未加密
- 缺少单元测试和集成测试
- 数据初始化回调（预设配置和默认设置）尚未实现

---

**适用范围**：第一期开发团队 | **状态**：第一期完工，准备第二期开发