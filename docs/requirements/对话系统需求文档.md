# QuickWrite AI 对话系统需求文档

## 1. 功能概述

AI 对话系统是 QuickWrite 的写作辅助核心模块，允许用户在写作工作台"对话"Tab 中与 AI 进行多轮对话。系统采用 **Android Service 集中管理所有会话逻辑** 的架构，UI 层仅负责展示和交互，所有上下文维护、消息管理、API 调用和 Tool 执行均在 Service 中完成。

### 1.1 核心特性

- **多会话并行**：支持同时进行多个独立对话，每个会话维持独立的上下文
- **Service 驱动**：对话逻辑完全在 Android Service 中运行，界面销毁/重建不中断对话
- **自动标题生成**：会话中首次用户请求后，静默发送标题生成请求
- **Function Calling**：向 AI 模型提供 Tool 集，使其可主动操作项目文件系统
- **流式响应**：支持 SSE (Server-Sent Events) 流式解析，逐 token 显示回复

### 1.2 在系统架构中的位置

```
┌──────────────────────────────────────────────┐
│  UI Layer (Compose)                          │
│  ChatTab -> AiChatViewModel -> Service 绑定   │
│  仅负责：消息展示、输入交互、会话切换            │
├──────────────────────────────────────────────┤
│  AIChatService (Foreground Service)          │
│  负责：会话管理、消息流、API 调用、Tool 执行    │
│  独立于 Activity 生命周期                      │
├──────────────────────────────────────────────┤
│  Data Layer                                  │
│  AiApiService (Retrofit)  +  Room 持久化       │
└──────────────────────────────────────────────┘
```

---

## 2. Service 架构设计

### 2.1 AIChatService 核心职责

`AIChatService` 是一个 Android Foreground Service，承载所有对话核心逻辑：

| 职责 | 说明 |
|------|------|
| 会话生命周期管理 | 创建/销毁/切换会话 |
| 上下文维护 | 每个会话的消息历史、系统提示词 |
| API 请求发送 | 调用 OpenAI 兼容 API |
| 流式响应解析 | SSE 流解析，逐 token 推送 |
| Tool 调度与执行 | AI 模型调用的 Tool → 本地执行 → 结果回传 |
| 自动标题生成 | 首次消息后生题 |
| 持久化同步 | 消息写入 Room 数据库 |

**Service 不应负责的事项**：
- UI 渲染（由 Compose 负责）
- 文本编辑（在编辑 Tab 中完成）
- 项目文件创建/修改 → 通过 Tool 机制委托给 FileManager/Repository

### 2.2 与 UI 的通信机制

使用 **Binder + StateFlow** 双向通信：

```
┌─────────────────┐         Binder IPC         ┌─────────────────┐
│   Compose UI    │ ◄──────────────────────────► │  AIChatService  │
│ AiChatViewModel │  - bindService()             │                 │
│                 │  - ServiceConnection          │  SessionManager │
│                 │  - StateFlow 订阅             │  ApiDispatcher  │
│                 │  - 方法调用（send/stop/switch）│  ToolExecutor   │
└─────────────────┘                             └─────────────────┘
```

**Service 暴露的 Binder 接口** (`IChatService`)：

```kotlin
interface IChatService {
    // 会话管理
    fun createSession(projectId: String, systemPrompt: String?, modelConfigId: Int?): String  // 返回 sessionId
    fun deleteSession(sessionId: String)
    fun switchToSession(sessionId: String)
    fun getSessionList(): List<SessionSummary>
    fun getSessionDetail(sessionId: String): SessionDetail?
    fun renameSession(sessionId: String, title: String)

    // 消息操作
    fun sendMessage(sessionId: String, content: String, attachedFiles: List<String> = emptyList())
    fun stopGeneration(sessionId: String)
    fun retryLastMessage(sessionId: String)
    fun deleteMessage(sessionId: String, messageIndex: Int)

    // 实时状态
    fun observeSessionState(sessionId: String): StateFlowWrapper<SessionState>
    fun observeSessionList(): StateFlowWrapper<List<SessionSummary>>
}
```

### 2.3 会话上下文结构

每个会话维护以下上下文信息，在每次 API 请求时组装为 messages 数组发送：

```kotlin
data class SessionContext(
    val sessionId: String,
    val projectId: String,
    val title: String,                    // 会话标题（自动生成或用户重命名）
    val systemPrompt: String,             // 系统提示词
    val modelConfigId: Int,               // 使用的 AI 模型配置 ID
    val messages: List<ChatMessage>,      // 完整消息历史
    val createdAt: Long,
    val updatedAt: Long,
    val isGenerating: Boolean             // 是否正在接收流式响应
)

data class ChatMessage(
    val id: Long,
    val role: MessageRole,               // system / user / assistant / tool
    val content: String,
    val tokenCount: Int = 0,
    val toolCalls: List<ToolCall>? = null,  // assistant 可能发起 tool call
    val toolCallId: String? = null,         // tool 消息关联的 call id
    val timestamp: Long
)

enum class MessageRole { SYSTEM, USER, ASSISTANT, TOOL }
```

**上下文组装策略**（每次 API 请求时）：
1. 第一条消息：`system` — 系统提示词
2. 后续消息：按时间顺序排列的 `user` / `assistant` / `tool` 消息
3. Token 总量超过模型上下文窗口时，从最早的消息开始截断（保留 system prompt 和最近的 N 条消息）

### 2.4 系统提示词构建

每次创建会话时，根据项目信息自动构建系统提示词：

```kotlin
fun buildSystemPrompt(project: ProjectEntity, rootDir: String): String {
    return """
你是一位专业的小说创作助手，正在帮助作者创作《${project.title}》。

## 项目基本信息
- 作者：${project.author}
- 类型：${project.genre}
- 项目路径：${rootDir}

## 项目目录结构
- 正文/ — 小说正文章节
- 设定/人物/ — 人物设定文档
- 设定/地点/ — 地点设定文档
- 设定/组织/ — 组织设定文档
- 设定/物品/ — 物品设定文档
- 时间线/ — 时间线及事件记录
- 记录/ — 创作过程记录
- 配置/ — AI指令与写作规范

## 工作原则
1. 所有回答基于项目文档中的设定信息
2. 遇到不确定的信息时，使用提供的工具主动查找相关文档
3. 确保建议内容与现有设定不冲突
4. 优先使用文档中已有信息，避免凭空创造

## 可用工具
你拥有文件系统读写权限，可以通过函数调用工具来：
- 浏览项目目录结构
- 读取和修改文件内容
- 搜索项目文档
- 创建和删除文件
""".trimIndent()
}
```

---

## 3. 会话管理

### 3.1 多会话并行

`SessionManager` 是 Service 内部组件，管理所有活跃会话：

```kotlin
class SessionManager(
    private val conversationRepository: AiConversationRepository,
    private val apiDispatcher: ApiDispatcher,
    private val toolExecutor: ToolExecutor
) {
    // 内存中的活跃会话映射
    private val activeSessions: MutableMap<String, SessionContext> = ConcurrentHashMap()
    
    // 每个会话的流式状态
    private val sessionStates: MutableMap<String, MutableStateFlow<SessionState>> = ConcurrentHashMap()
    
    fun createSession(...): String { }
    fun sendMessage(sessionId: String, content: String) { }
    fun stopGeneration(sessionId: String) { }
    fun switchSession(sessionId: String) { }
    // ...
}
```

**并行策略**：
- 每个会话有独立的 `CoroutineScope`，互不干扰
- API 请求使用独立的 OkHttp Dispatcher，不同会话可并行发起请求
- 同一会话内的消息串行处理（上一条回复完才能发下一条）
- 正在生成回复的会话可调用 `stopGeneration()` 中断

### 3.2 自动标题生成

**触发时机**：会话中首次用户消息发送后，AI 返回第一条回复时。

**流程**：
1. 用户发送第一条消息，AI 正常生成回复
2. 回复完成后，SessionManager 检测到这是该会话的第一条回复（`messages` 中此前只有 1 条 user 消息）
3. 自动构建静默请求：`"请用不超过10个字为以上对话生成一个标题，只输出标题文本，不要包含引号或任何解释。"`
4. 该请求不追加到 UI 消息列表中（标记 `silent = true`），仅将返回结果设置为会话标题
5. 标题更新通过 `sessionStates` StateFlow 推送到 UI

**静默消息标记**：

```kotlin
data class ChatMessage(
    // ...
    val silent: Boolean = false   // 静默消息不显示在 UI 中
)
```

**标题生成失败处理**：使用截断的用户消息首行作为默认标题（如 `"帮我写一段..."`）。

### 3.3 会话持久化

会话和消息同步写入 Room 数据库，支持应用重启后恢复：

| 表 | 存储内容 |
|----|---------|
| `ai_sessions` | 会话元数据（标题、项目ID、模型配置、创建时间） |
| `ai_messages` | 消息记录（角色、内容、token数、静默标记） |

**持久化时机**：
- 会话创建时：写入 `ai_sessions`
- 用户消息发送时：立即写入 `ai_messages`
- AI 回复完整接收时：写入 `ai_messages`
- 流式中间 token：仅内存，不写入数据库
- 标题生成完成时：更新 `ai_sessions.title`

### 3.4 会话状态定义

```kotlin
sealed class SessionState {
    object Idle : SessionState()                                    // 空闲，等待用户输入
    data class Generating(val partialContent: String) : SessionState()  // AI 正在生成
    data class Error(val message: UiText) : SessionState()             // 出错
}

data class SessionSummary(
    val sessionId: String,
    val title: String,
    val lastMessage: String,          // 最后一条消息摘要
    val messageCount: Int,
    val createdAt: Long,
    val updatedAt: Long,
    val isActive: Boolean             // 是否有正在进行的生成
)
```

---

## 4. 会话列表 UI

### 4.1 入口变更

**当前行为**：对话页右上角有一个 Menu/Close 按钮用于切换章节列表的显示/隐藏（编辑 Tab 功能）。

**新行为**：在对话 Tab 下，右上角按钮改为 **展开会话列表**。

```
┌──────────────────────────────────┐
│  ← 返回   项目标题          [≡]  │  ← ≡ 按钮：在对话Tab下展开会话列表
├──────────────────────────────────┤
│  [编辑]  [对话]                  │  
├──────────────────────────────────┤
│                                  │
│         对话内容区域              │
│                                  │
├──────────────────────────────────┤
│  [输入框]                    [发送] │
└──────────────────────────────────┘
```

点击 `≡` 后，从右侧滑出会话列表面板（Modal Drawer 或 Bottom Sheet 风格，视屏幕空间决定）：

```
┌──────────────────────────┬─────────┐
│                          │ 会话列表 │
│       对话内容            │ ─────── │
│                          │ ★ 大纲讨论│  ← 当前活跃会话高亮
│                          │   人物设定│
│                          │   情节构思│
│                          │ ─────── │
│                          │ [+ 新会话]│
├──────────────────────────┴─────────┤
│  [输入框]                    [发送] │
└────────────────────────────────────┘
```

### 4.2 会话列表组件

**功能要求**：
- 显示所有会话，按 `updatedAt` 倒序排列
- 当前活跃会话高亮显示
- 每条显示：标题、最后消息时间、消息数
- 长按会话可删除（弹出确认对话框）
- 点击会话切换到该会话
- 底部 `+ 新会话` 按钮创建新会话
- 支持重命名（点击标题进入编辑）

**Tab 区分**：
- **编辑 Tab**：右上角按钮行为不变（展开/收起章节列表）
- **对话 Tab**：右上角按钮变更为展开会话列表

---

## 5. Function Calling — AI 工具集

### 5.1 工具架构

Service 中的 `ToolExecutor` 负责注册和执行工具。工具定义遵循 OpenAI Function Calling 规范，在每次 API 请求的 `tools` 参数中声明。

```kotlin
interface ChatTool {
    /** 工具定义（符合 OpenAI function schema） */
    val definition: ToolDefinition
    
    /** 执行工具调用，返回结果文本 */
    suspend fun execute(arguments: JSONObject, context: ToolContext): String
}

data class ToolDefinition(
    val name: String,
    val description: String,
    val parameters: JSONObject   // JSON Schema 格式的参数定义
)

data class ToolContext(
    val projectId: String,
    val fileManager: FileManager,
    val projectRepository: ProjectRepository,
    val projectManagementUseCase: ProjectManagementUseCase
)
```

### 5.2 工具清单

以下 13 个工具供 AI 模型调用：

#### T1. `get_project_list` — 获取项目列表

| 属性 | 值 |
|------|-----|
| **名称** | `get_project_list` |
| **描述** | 获取当前应用中所有项目的列表，包含项目基本信息（ID、标题、作者、类型、状态等） |
| **参数** | 无 |
| **执行** | 调用 `ProjectRepository.getAllProjects()` 返回项目列表 |
| **返回格式** | JSON 数组，包含每个项目的 id、title、author、genre、status、createdTime、modifiedTime |

#### T2. `get_project_info` — 获取项目信息

| 属性 | 值 |
|------|-----|
| **名称** | `get_project_info` |
| **描述** | 获取指定项目的详细信息，包括元数据和目录统计 |
| **参数** | `projectId: string` — 项目 ID |
| **执行** | 调用 `ProjectRepository.getProjectById()` + `FileManager` 获取目录统计 + 读取 `info.json` |
| **返回格式** | JSON 对象，含 project 元数据、目录文件统计、info.json 内容 |

#### T3. `get_folder_structure` — 获取项目文件夹结构

| 属性 | 值 |
|------|-----|
| **名称** | `get_folder_structure` |
| **描述** | 获取项目中指定路径下的完整文件夹结构（文件名列表、目录树），用于了解项目文档组织 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` (可选，默认 "") — 相对于项目根目录的路径 |
| **执行** | 基于 `project.storagePath` + `relativePath` 遍历目录，返回文件/目录列表（不含文件内容） |
| **安全** | 调用 `FileManager.isPathSafe()` 验证路径，禁止目录遍历攻击 |
| **返回格式** | JSON 树形结构，每项含 name、type(file/dir)、size、lastModified |

#### T4. `view_file` — 查看文件内容

| 属性 | 值 |
|------|-----|
| **名称** | `view_file` |
| **描述** | 读取项目中指定文件的内容，可指定行号范围以限制返回长度 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` — 相对于项目根目录的文件路径；`startLine: integer` (可选，默认 1) — 起始行号；`endLine: integer` (可选，默认 0=全部) — 结束行号 |
| **执行** | 基于 `project.storagePath` + `relativePath` 读取文件，按行号范围截取 |
| **返回格式** | JSON，含 filePath、totalLines、startLine、endLine、content（行号前缀）、language（扩展名推断） |
| **限制** | 单次返回最多 500 行；超出时截断并在末尾标注 |

#### T5. `edit_file` — 修改文件内容

| 属性 | 值 |
|------|-----|
| **名称** | `edit_file` |
| **描述** | 替换项目中指定文件的特定行号范围内容，支持精确行级编辑 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` — 文件路径；`startLine: integer` — 替换起始行号；`endLine: integer` — 替换结束行号；`newContent: string` — 替换的新内容（多行文本） |
| **执行** | 读取原文件所有行 → 替换 [startLine, endLine] 范围 → 写回文件 |
| **边界处理** | startLine/endLine 超出文件行数时自动 clamp 到有效范围；endLine 为 -1 时替换到文件末尾 |
| **返回** | JSON，含 filePath、replacedRange、newContent、lineCountAfter |

#### T6. `delete_file` — 删除文件

| 属性 | 值 |
|------|-----|
| **名称** | `delete_file` |
| **描述** | 删除项目中指定路径的文件或空目录 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` — 文件/目录路径 |
| **执行** | 调用 `FileManager.deleteFileOrDirectory()` |
| **安全** | 禁止删除项目根目录本身和核心目录（正文/、设定/、时间线/等的一级目录） |
| **返回** | JSON，含 deleted、path |

#### T7. `create_file` — 创建文件

| 属性 | 值 |
|------|-----|
| **名称** | `create_file` |
| **描述** | 在项目中指定路径创建新文件，可同时写入初始内容 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` — 文件路径；`content: string` (可选) — 初始文件内容 |
| **执行** | 调用 `FileManager.createFile()` → 如有 content 则调用 `writeFileContent()` |
| **自动行为** | 自动创建不存在的父目录；若文件已存在则返回错误 |
| **返回** | JSON，含 created、path、size |

#### T8. `move_file` — 移动项目内文件

| 属性 | 值 |
|------|-----|
| **名称** | `move_file` |
| **描述** | 将项目内文件或目录移动到新路径（可同时重命名），源路径和目标路径必须在同一项目内 |
| **参数** | `projectId: string` — 项目 ID；`sourcePath: string` — 源文件/目录的相对路径；`targetPath: string` — 目标相对路径（含新名称） |
| **执行** | 调用 `FileManager.renameFileOrDirectory()`，基于 `project.storagePath` 拼接完整路径 |
| **安全** | 源路径和目标路径均需通过 `isPathSafe()` 验证；禁止移动核心一级目录（正文/、设定/、时间线/、记录/、配置/） |
| **行为** | 自动创建目标路径的父目录；若目标已存在则返回错误 |
| **返回** | JSON，含 moved、sourcePath、targetPath |

#### T9. `copy_file` — 复制项目内文件

| 属性 | 值 |
|------|-----|
| **名称** | `copy_file` |
| **描述** | 复制项目内文件到新路径（不支持复制目录），源文件和目标路径必须在同一项目内 |
| **参数** | `projectId: string` — 项目 ID；`sourcePath: string` — 源文件的相对路径；`targetPath: string` — 目标相对路径（含新文件名） |
| **执行** | 读取源文件内容 → 写入到目标路径（基于 `project.storagePath` 拼接完整路径） |
| **安全** | 源路径和目标路径均需通过 `isPathSafe()` 验证；仅支持文件复制，源为目录时返回错误 |
| **行为** | 自动创建目标路径的父目录；若目标已存在则返回错误 |
| **返回** | JSON，含 copied、sourcePath、targetPath、size |

#### T10. `search_in_project` — 搜索项目文件

| 属性 | 值 |
|------|-----|
| **名称** | `search_in_project` |
| **描述** | 在项目指定路径下递归搜索文件内容，支持关键字和正则表达式 |
| **参数** | `projectId: string` — 项目 ID；`relativePath: string` (可选，默认 ""=项目根目录) — 搜索起始目录；`query: string` — 搜索关键字或正则表达式；`useRegex: boolean` (可选，默认 false) — 是否将 query 按正则表达式处理；`maxResults: integer` (可选，默认 20) — 最大返回结果数 |
| **执行** | 在 `project.storagePath/relativePath` 下递归遍历所有 .md/.txt/.json 文件，按 query 匹配内容 |
| **返回** | JSON 数组，每项含 filePath、lineNumber、lineContent、matchText |
| **性能** | 跳过二进制文件和超过 1MB 的文本文件 |

#### T11. `update_project_info` — 修改项目信息

| 属性 | 值 |
|------|-----|
| **名称** | `update_project_info` |
| **描述** | 修改项目的基本信息（标题、作者、类型、描述等），同时更新 Room 数据库和 info.json |
| **参数** | `projectId: string` — 项目 ID；`fields: object` — 要更新的字段键值对，支持：title、author、genre、description、status |
| **执行** | 更新 `ProjectEntity` → 调用 `ProjectRepository.updateProject()` → 更新 `info.json` |
| **返回** | JSON，含 updated、projectId、changedFields |
| **限制** | 仅允许修改指定字段，不允许修改 id、createdTime、storagePath 等 |

#### T12. `create_project` — 创建项目

| 属性 | 值 |
|------|-----|
| **名称** | `create_project` |
| **描述** | 创建新的小说项目，自动生成目录结构和元数据文件 |
| **参数** | `title: string` — 小说标题；`author: string` — 作者名；`genre: string` — 小说类型；`description: string` (可选) — 项目描述 |
| **执行** | 调用 `ProjectManagementUseCase.createProject()`，生成 UUID、创建目录结构、写入 info.json、插入 Room 记录 |
| **返回** | JSON，含 created、projectId、title、storagePath |

#### T13. `delete_project` — 删除项目

| 属性 | 值 |
|------|-----|
| **名称** | `delete_project` |
| **描述** | 删除指定项目，包括数据库记录和所有文件 |
| **参数** | `projectId: string` — 项目 ID；`confirmTitle: string` — 必须提供项目标题以确认删除 |
| **执行** | 验证 confirmTitle 与项目标题匹配 → 调用 `ProjectManagementUseCase.deleteProject()` |
| **安全** | 双重验证，防止误删；confirmTitle 不匹配则拒绝并返回错误 |
| **返回** | JSON，含 deleted、projectId、title |

### 5.3 Tool Call 执行流程

```
用户消息
    │
    ▼
┌──────────────────────────────────────┐
│  发送到 AI API（含 tools 定义 + 上下文）│
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────┐    是     ┌─────────────────┐
│ 响应包含 tool_calls？     │─────────► │ ToolExecutor    │
└──────────┬───────────────┘           │ 逐个执行 tool    │
           │ 否                         │ 收集结果         │
           ▼                            └────────┬────────┘
┌──────────────────────────┐                    │
│ 直接返回文本内容           │                    ▼
│ 推送到 UI                 │           ┌──────────────────┐
└──────────────────────────┘           │ 将 tool 结果作为    │
                                       │ tool role 消息     │
                                       │ 追加到上下文       │
                                       │ 再次发送到 AI API  │
                                       └────────┬──────────┘
                                                │
                                                ▼
                                       ┌──────────────────┐
                                       │ 继续循环直到       │
                                       │ AI 返回最终文本    │
                                       └──────────────────┘
```

**Tool Call 循环终止条件**：
- AI 返回不包含 `tool_calls` 的文本响应 → 正常结束
- 循环超过 10 轮 → 强制终止并返回错误
- 用户调用 `stopGeneration()` → 中断

**Tool 结果消息**：每个 Tool Call 结果以 `role: "tool"` 消息追加到上下文中，`tool_call_id` 关联到对应的 Assistant tool call。

---

## 6. 网络层设计

### 6.1 AiApiService (Retrofit)

```kotlin
interface AiApiService {
    @POST("/v1/chat/completions")
    suspend fun chatCompletion(
        @Header("Authorization") authorization: String,
        @Body request: ChatCompletionRequest
    ): ChatCompletionResponse

    @POST("/v1/chat/completions")
    @Streaming
    suspend fun chatCompletionStream(
        @Header("Authorization") authorization: String,
        @Body request: ChatCompletionRequest
    ): ResponseBody   // SSE 流，逐行解析
}

data class ChatCompletionRequest(
    val model: String,
    val messages: List<MessageDto>,
    val temperature: Float = 0.7f,
    val max_tokens: Int = 4096,
    val stream: Boolean = true,
    val tools: List<ToolDefinition>? = null,
    val tool_choice: String? = "auto"
)

data class MessageDto(
    val role: String,
    val content: String?,
    val tool_calls: List<ToolCallDto>? = null,
    val tool_call_id: String? = null
)

data class ChatCompletionResponse(
    val id: String,
    val choices: List<ChoiceDto>,
    val usage: UsageDto?
)

data class ChoiceDto(
    val index: Int,
    val message: MessageDto,
    val delta: DeltaDto?,       // 流式响应使用 delta
    val finish_reason: String?
)

data class DeltaDto(
    val role: String?,
    val content: String?,
    val tool_calls: List<ToolCallChunkDto>?
)
```

### 6.2 SSE 流式解析器

`StreamParser` 工具类，解析 SSE 格式的流式响应：

```kotlin
class StreamParser {
    /**
     * 解析 SSE 数据行，提取 content delta
     * 格式: data: {"choices":[{"delta":{"content":"文"}}]}
     *        data: [DONE]
     */
    fun parseLine(line: String): StreamChunk
    
    /**
     * 解析完整的 tool_calls delta（跨多个 chunk 拼接）
     */
    fun parseToolCallDelta(delta: DeltaDto): ToolCallChunk?
}

sealed class StreamChunk {
    data class Content(val text: String) : StreamChunk()
    data class ToolCallBegin(val id: String, val name: String) : StreamChunk()
    data class ToolCallArgs(val id: String, val argsDelta: String) : StreamChunk()
    data class Done(val usage: UsageDto?) : StreamChunk()
    data class Error(val message: String) : StreamChunk()
}
```

### 6.3 请求配置

- **Base URL**：从 `AiModelConfigEntity.baseUrl` 读取
- **API Key**：从 `encrypted_ai_model_configs` 表解密读取（升级后加密存储）
- **Model Name**：从配置中读取
- **Timeout**：connect 15s，read 120s（允许长时间流式响应）
- **Dispatcher**：每个 Service 实例拥有独立的 OkHttp Dispatcher（max 5 并发请求）

---

## 7. 数据库设计

### 7.1 新增 Entity

#### ai_sessions 表

```kotlin
@Entity(
    tableName = "ai_sessions",
    foreignKeys = [
        ForeignKey(
            entity = ProjectEntity::class,
            parentColumns = ["id"],
            childColumns = ["project_id"],
            onDelete = ForeignKey.CASCADE
        )
    ],
    indices = [
        Index(value = ["project_id", "updated_at"], name = "idx_sessions_project"),
        Index(value = ["session_id"], unique = true, name = "idx_sessions_id")
    ]
)
data class AiSessionEntity(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0,
    @ColumnInfo(name = "session_id")
    val sessionId: String,              // UUID
    @ColumnInfo(name = "project_id")
    val projectId: String,
    @ColumnInfo(name = "title")
    val title: String,                  // 会话标题（自动生成或用户修改）
    @ColumnInfo(name = "model_config_id")
    val modelConfigId: Int,
    @ColumnInfo(name = "system_prompt")
    val systemPrompt: String,
    @ColumnInfo(name = "created_at")
    val createdAt: Long,
    @ColumnInfo(name = "updated_at")
    val updatedAt: Long
)
```

#### ai_messages 表

```kotlin
@Entity(
    tableName = "ai_messages",
    foreignKeys = [
        ForeignKey(
            entity = AiSessionEntity::class,
            parentColumns = ["session_id"],
            childColumns = ["session_id"],
            onDelete = ForeignKey.CASCADE
        )
    ],
    indices = [
        Index(value = ["session_id", "created_at"], name = "idx_messages_session"),
        Index(value = ["session_id", "message_order"], name = "idx_messages_order")
    ]
)
data class AiMessageEntity(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0,
    @ColumnInfo(name = "session_id")
    val sessionId: String,
    @ColumnInfo(name = "message_order")
    val messageOrder: Int,              // 消息序号（0-based）
    @ColumnInfo(name = "role")
    val role: String,                   // system / user / assistant / tool
    @ColumnInfo(name = "content")
    val content: String,
    @ColumnInfo(name = "token_count")
    val tokenCount: Int = 0,
    @ColumnInfo(name = "tool_call_id")
    val toolCallId: String? = null,     // tool 消息关联的 call id
    @ColumnInfo(name = "tool_calls_json")
    val toolCallsJson: String? = null,  // assistant 的 tool_calls 序列化 JSON
    @ColumnInfo(name = "is_silent")
    val isSilent: Boolean = false,      // 静默消息（标题生成请求不显示）
    @ColumnInfo(name = "created_at")
    val createdAt: Long
)
```

### 7.2 新增 DAO

```kotlin
@Dao
interface AiSessionDao {
    @Query("SELECT * FROM ai_sessions WHERE project_id = :projectId ORDER BY updated_at DESC")
    fun getSessionsByProject(projectId: String): Flow<List<AiSessionEntity>>

    @Query("SELECT * FROM ai_sessions WHERE session_id = :sessionId")
    suspend fun getSessionById(sessionId: String): AiSessionEntity?

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertSession(session: AiSessionEntity)

    @Update
    suspend fun updateSession(session: AiSessionEntity)

    @Query("DELETE FROM ai_sessions WHERE session_id = :sessionId")
    suspend fun deleteSession(sessionId: String)

    @Query("UPDATE ai_sessions SET title = :title WHERE session_id = :sessionId")
    suspend fun updateSessionTitle(sessionId: String, title: String)
}

@Dao
interface AiMessageDao {
    @Query("SELECT * FROM ai_messages WHERE session_id = :sessionId ORDER BY message_order ASC")
    suspend fun getMessagesBySession(sessionId: String): List<AiMessageEntity>

    @Query("SELECT * FROM ai_messages WHERE session_id = :sessionId AND is_silent = 0 ORDER BY message_order ASC")
    fun getVisibleMessagesBySession(sessionId: String): Flow<List<AiMessageEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertMessage(message: AiMessageEntity)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertMessages(messages: List<AiMessageEntity>)

    @Query("DELETE FROM ai_messages WHERE session_id = :sessionId AND message_order >= :fromOrder")
    suspend fun deleteMessagesFrom(sessionId: String, fromOrder: Int)

    @Query("DELETE FROM ai_messages WHERE session_id = :sessionId")
    suspend fun deleteMessagesBySession(sessionId: String)

    @Query("""
        SELECT COUNT(*) FROM ai_messages 
        WHERE session_id = :sessionId AND is_silent = 0 AND role = 'user'
    """)
    suspend fun getUserMessageCount(sessionId: String): Int
}
```

### 7.3 数据库升级

| 版本 | 变更 |
|------|------|
| v1 → v2 | 新增 `ai_sessions` 表、`ai_messages` 表 |

**Migration v1→v2**：纯新增表，无数据迁移需求，直接 `CREATE TABLE`。

---

## 8. UI 层设计

### 8.1 ChatTab Composable 结构

```
ChatTab
├── SessionListDrawer           # 会话列表侧边栏（ModalDrawerSheet）
│   ├── SessionListItem * N     # 每条会话项
│   └── NewSessionButton        # 新建会话按钮
├── MessageList                 # 消息列表（LazyColumn）
│   ├── ChatBubble * N          # 消息气泡
│   │   ├── UserBubble          # 用户消息（右侧，蓝色）
│   │   ├── AssistantBubble     # AI 回复（左侧，灰色）
│   │   │   ├── TextContent     # 文本内容（Markdown 渲染）
│   │   │   ├── ToolCallCard    # Tool 调用展示（可折叠）
│   │   │   └── LoadingDots     # 生成中动画
│   │   └── ToolResultBubble    # Tool 执行结果（左侧，浅绿）
│   └── AutoScrollHandler       # 自动滚动到底部
├── ContextIndicator            # 上下文指示条（可折叠）
│   └── 显示："当前参考：人物/张无忌.md · 时间线/总览.md"
├── InputArea                   # 输入区域
│   ├── TextField               # 多行输入框
│   ├── AttachButton            # 附加文件选择
│   ├── StopButton              # 停止生成（生成中显示）
│   └── SendButton              # 发送按钮
└── EmptyState                  # 无会话时的引导界面
```

### 8.2 AiChatViewModel

ViewModel 仅作为 UI 状态持有者和 Service 绑定的桥梁：

```kotlin
class AiChatViewModel(
    private val projectId: String,
    // 通过 AppContainer 获取 Service 绑定
) : ViewModel() {
    
    // UI 状态
    val sessionList: StateFlow<List<SessionSummary>>
    val currentSessionState: StateFlow<SessionState>
    val currentMessages: StateFlow<List<ChatMessage>>    // 仅非静默消息
    val isSessionListVisible: StateFlow<Boolean>
    
    // UI 操作
    fun createNewSession()
    fun switchSession(sessionId: String)
    fun deleteSession(sessionId: String)
    fun sendMessage(content: String, attachedFiles: List<String> = emptyList())
    fun stopGeneration()
    fun retryLastMessage()
    fun toggleSessionList()
    
    // 生命周期
    override fun onCleared() {
        // 解绑 Service（但不停止 Service）
    }
}
```

### 8.3 消息气泡组件

**用户消息气泡**：
- 右对齐，蓝色背景 `#1a237e`（主题色），白色文字
- 显示发送时间
- 支持长按复制

**AI 回复气泡**：
- 左对齐，浅灰背景 `#f5f5f5`，深色文字
- Markdown 格式渲染（标题、列表、代码块、粗体、斜体）
- 流式生成中：逐字显示，末尾闪烁光标
- 流式完成后：显示完整内容
- 支持长按复制/重试

**Tool Call 卡片**（嵌入在 AI 回复气泡中）：
- 可折叠，默认折叠
- 标题："🔧 正在执行: view_file（正文/第一章.md）"
- 展开后显示执行结果摘要

**生成中状态**：
- 三条跳动的点动画（`...`）
- 底部输入区发送按钮变为停止按钮

### 8.4 上下文指示条

显示在消息列表顶部，指示当前 AI 上下文参考了哪些文件：

```
┌──────────────────────────────────────────┐
│ 📁 上下文: 人物/张无忌.md · 写作规范.md   ▼ │
└──────────────────────────────────────────┘
```

- 可点击展开查看完整上下文文件列表
- 每次 AI 回复后更新
- 背景色为浅蓝色半透明

### 8.5 空状态引导

当项目尚无任何会话时，显示引导界面：

```
┌──────────────────────────┐
│                          │
│       💬                 │
│   开始与 AI 对话          │
│                          │
│  AI 可以帮你：            │
│  · 续写章节内容          │
│  · 讨论人物设定           │
│  · 构思情节发展           │
│  · 润色文笔              │
│                          │
│  [新建对话]               │
│                          │
└──────────────────────────┘
```

---

## 9. Service 生命周期管理

### 9.1 启动与绑定

```kotlin
// 在需要时启动 Service（如进入 ChatTab）
context.startForegroundService(Intent(context, AIChatService::class.java))
context.bindService(chatServiceIntent, connection, Context.BIND_AUTO_CREATE)

// UI 销毁时解绑，但不停止 Service
context.unbindService(connection)

// 项目切换或离开写作页面时，可选择保持 Service 运行一段时间
// 无活跃会话 5 分钟后自动停止
```

### 9.2 前台服务通知

Service 运行期间显示前台通知：
- **无生成任务时**："QuickWrite AI 助手 — 等待中"
- **生成中**："QuickWrite AI 助手 — 正在生成回复..."
- **通知操作**：停止生成、打开应用

### 9.3 自动回收

- 所有会话空闲超过 5 分钟后，Service 自行 `stopSelf()`
- 用户重新进入对话 Tab 时，重新启动 Service 并从数据库恢复会话

---

## 10. 错误处理

### 10.1 网络错误

| 错误类型 | 处理策略 |
|---------|---------|
| 网络不可用 | 显示 Snackbar "网络不可用"，消息保留在输入框中不丢失 |
| 请求超时 | 自动重试 1 次，仍失败则显示错误；用户可手动重试 |
| API 返回 4xx | 显示具体错误信息（如 "API Key 无效"） |
| API 返回 5xx | 显示 "AI 服务暂时不可用，请稍后再试"，支持重试 |
| 流式中断 | 保留已接收的部分内容，显示 "连接中断"，提供重试按钮 |

### 10.2 Tool 执行错误

| 错误类型 | 返回给 AI 的内容 |
|---------|----------------|
| 路径不安全 | `Error: 路径不在项目范围内` |
| 文件不存在 | `Error: 文件不存在: {path}` |
| 权限不足 | `Error: 无权限操作此文件` |
| 执行异常 | `Error: 操作失败: {exception message}` |

Tool 执行错误不阻断对话，将错误信息作为 tool 结果返回给 AI，由 AI 决定下一步操作。

### 10.3 UI 错误状态

- 错误消息通过 Snackbar 显示
- 发送失败的消息保留在输入框中
- 重试按钮出现在失败消息气泡旁

---

## 11. 安全性考虑

- **路径安全**：所有文件操作前调用 `FileManager.isPathSafe()` 验证，防止目录遍历
- **项目隔离**：工具只能操作指定 projectId 的文件，不能跨项目访问
- **删除保护**：项目删除需确认标题；禁止删除核心目录
- **API Key 安全**：升级到加密存储（EncryptedSharedPreferences 或 Android Keystore）
- **输入验证**：所有用户输入和 AI 返回内容进行适当的清理

---

## 12. 验收标准

### 12.1 核心功能

- [ ] 可以在对话 Tab 中创建新会话
- [ ] 会话列表正确显示，可以切换和删除会话
- [ ] 对话 Tab 右上角按钮展开会话列表（非编辑 Tab 的章节列表）
- [ ] 发送消息后能收到 AI 流式回复（逐字显示）
- [ ] 支持并行多个会话，切换后上下文不混淆
- [ ] 首次消息后自动生成会话标题
- [ ] 中断生成功能正常（停止按钮）
- [ ] 会话和消息在应用重启后正确恢复

### 12.2 Function Calling

- [ ] AI 可以调用 `view_file` 读取文件内容
- [ ] AI 可以调用 `edit_file` 修改文件
- [ ] AI 可以调用 `create_file` / `delete_file` 创建/删除文件
- [ ] AI 可以调用 `search_in_project` 搜索项目内容
- [ ] AI 可以调用 `get_project_list` 列出所有项目
- [ ] AI 可以调用 `get_project_info` 获取项目详情
- [ ] AI 可以调用 `get_folder_structure` 浏览目录结构
- [ ] AI 可以调用 `update_project_info` 修改项目信息
- [ ] AI 可以调用 `create_project` / `delete_project` 创建/删除项目
- [ ] Tool Call 执行失败时 AI 能优雅处理并告知用户

### 12.3 稳定性

- [ ] 网络中断时不会崩溃，错误提示友好
- [ ] 切换 Tab 时正在生成的回复不中断
- [ ] Service 在合理时机自动回收
- [ ] 长对话历史正确截断，不导致 token 超限

### 12.4 UI 体验

- [ ] 消息列表自动滚动到底部
- [ ] 流式生成动画流畅无卡顿
- [ ] Markdown 渲染正确（标题/列表/粗体/代码块）
- [ ] 暗色模式适配
- [ ] 平板适配

---

## 13. 开发优先级

| 阶段 | 内容 | 预估周期 |
|------|------|---------|
| 阶段 1 | Service 架构 + 网络层 + 流式解析 + 基础消息收发 | 1-2 周 |
| 阶段 2 | 会话管理（创建/切换/删除/持久化）+ 自动标题生成 | 1 周 |
| 阶段 3 | Function Calling 工具集实现（13 个工具） | 1-2 周 |
| 阶段 4 | ChatTab UI 完善（气泡/Markdown渲染/会话列表/上下文指示） | 1 周 |
| 阶段 5 | 错误处理/边界情况/测试 | 1 周 |

---

## 14. 技术依赖

| 依赖 | 用途 | 状态 |
|------|------|------|
| Retrofit + OkHttp | AI API 网络请求 | ✅ 已在 libs.versions.toml |
| OkHttp SSE | 流式响应解析 | ✅ OkHttp 已包含 |
| Room (ai_sessions, ai_messages) | 会话和消息持久化 | ⏳ 待新增表 |
| Kotlin Coroutines + Flow | 异步和响应式数据流 | ✅ 已使用 |
| Android Foreground Service | Service 生命周期 | ⏳ 待创建 |
| Markdown 渲染库 | AI 回复中的 Markdown 渲染 | ⏳ 待选型 |

---

**文档版本**: 1.0  
**最后更新**: 2026-04-29  
**作者**: AI Agent  
**适用范围**: 第二期 — AI 对话与写作辅助模块
